<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="css/style.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <link href="https://fonts.googleapis.com/css?family=Open+Sans|Quicksand:400,700" rel="stylesheet">
    <title>codeKeeper Profile</title>
  </head>
  <body>
    <header>
      <h1 class="logo, button">{codeKeeper}</h1>
      <a class="floatRight" href="/profile">Return to your profile</a>

      <h2>Explore Code by Others</h2>

      <div class="">
        <p>Filter by language:</p>
        <a class="Javascript" href="/all/filter/Javascript">Javascript</a>
        <a class="HTML" href="/all/filter/HTML">HTML</a>
        <a class="CSS" href="/all/filter/CSS">CSS</a>
        <a class="Ruby" href="/all/filter/Ruby">Ruby</a>
        <a class="button" href="/">All</a>
      </div>

      <form class="" action="/all/search" method="post">
        <p>Search by tag:</p>
        <input type="search" name="tag" value="" placeholder="Search by tag">
        <button type="submit" name="button" id="loginButton">Submit</button>
      </form>

    </header>

    <div id="snip-container">
      {{#snip}}
        <div class="snip {{language}}">
          <a href='/profile/{{_id}}'><h2>Title: {{title}}</h2></a>
          <p>Created by: {{username}}</p>
          <a href='/all/filter/{{language}}'class='{{language}}'>{{language}}</a>
        </div>
      {{/snip}}
    </div>

    {{^snip}}
      <h1>Start adding {{language}} code snippets!</h1>
    {{/snip}}

  </body>
</html>
